<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="RTCDialog" parent="android:Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!-- 为了避免在有 NavigationBar 的手机上 Dialog 从 NavigationBar 底部上来。去掉 Dialog 的动画，使用 View 的动画。-->
        <item name="android:layout_width">match_parent</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="hintTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/alivc_rtc_color_text_gray</item>
        <item name="colorControlNormal">@color/alivc_rtc_color_text_gray</item>
        <item name="colorControlActivated">@color/alivc_rtc_color_text_gray</item>
        <item name="colorControlHighlight">@color/alivc_rtc_color_text_gray</item>
    </style>
</resources>